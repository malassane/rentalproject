<h1>Property list</h1>
<table>
  <tr>
    <th>Property</th>
    <th>Rent</th>
    <th>Address</th>
    <th>Building Age</th>
    <th>Remark</th>
  </tr>
<% @properties.each do |property| %>

  <tr>
    <td><%= property.propertyName %></td>
    <td><%= property.rent %></td>
    <td><%= property.address %></td>
    <td><%= property.buildAge %></td>
    <td><%= property.note %></td>
    <td><%= link_to 'Check the details', property_path(property.id) %></td>
    <td><%= link_to "Edit property", edit_property_path(property.id) %></td>
    <td><%= link_to 'supprimer', property_path(property.id), method: :delete ,data: { confirm: 'voulez vous supprimer？' } %></td>

  </tr>
<% end %>

</table>
<br>
<%= link_to 'Post a new property', new_property_path %>
